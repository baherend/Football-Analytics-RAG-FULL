# Architecture Documentation — Moved

This file is no longer maintained. It described a file layout that predates
competition portability and the `06_retrieve_context.py` → `src/retrieval/`
+ `src/query/` split, and had drifted out of sync with the actual codebase.

There is exactly one canonical architecture source for this repository:

**[`docs/architecture/overview.md`](docs/architecture/overview.md)**

See also `PROJECT_MEMORY.md` for current, factual repository state (what
exists today, as opposed to the target architecture the overview document
describes).

Do not maintain a second, competing architecture description here or
anywhere else. If this file is regenerated by tooling, replace it with this
pointer again rather than letting it accumulate independent content.
